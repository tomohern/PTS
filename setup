#!/bin/bash
#
#Update and install needed software
#
apt update
unattended-upgrades -d
apt install openjdk-8-jre-headless
apt install unzip
#
# Create new user to run PT as 
#
adduser $uname
usermod -aG sudo $uname
echo Please execute 'downloadLatestPT'
su - $uname
